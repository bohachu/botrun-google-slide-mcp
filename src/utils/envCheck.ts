// This file is no longer needed as we're using service account authentication
// Keeping it for backwards compatibility but the function does nothing
export const checkEnvironmentVariables = (): void => {
  // No environment variables needed for service account authentication
};
